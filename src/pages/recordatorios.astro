---
import { verifySession } from '../lib/session';
import AppHeader from '../components/AppHeader.astro';
import DeudasBoard from '../components/DeudasBoard';
import '../styles/global.css';

const session = await verifySession(Astro.cookies);
if (!session) return Astro.redirect('/login');
---

<!doctype html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Deudas — Copymax POS</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body class="min-h-screen bg-gray-100 font-quicksand">
  <AppHeader activePage="recordatorios" username={session.username} role={session.role} />

  <main class="max-w-2xl mx-auto px-4 py-6">
    <div class="mb-5">
      <h1 class="text-xl font-bold text-gray-800">Deudas</h1>
      <p class="text-sm text-gray-500">Clientes que quedaron a deber</p>
    </div>
    <DeudasBoard client:load />
  </main>
</body>
</html>
