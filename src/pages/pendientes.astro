---
import { verifySession } from '../lib/session';
import PendientesBoard from '../components/PendientesBoard';
import AppHeader from '../components/AppHeader.astro';
import '../styles/global.css';

const session = await verifySession(Astro.cookies);
if (!session) return Astro.redirect('/login');
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pendientes — Copymax POS</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body class="min-h-screen bg-gray-100">
    <AppHeader activePage="pendientes" username={session.username} role={session.role} />

    <main class="max-w-7xl mx-auto px-4 py-4" style="height: calc(100vh - 57px);">
      <PendientesBoard client:load username={session.username} />
    </main>
  </body>
</html>
